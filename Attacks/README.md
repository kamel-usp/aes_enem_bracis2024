GenerateAdversarial is the main file. It has two functions: one to generate the essays and one to return the name of the attacks.
ExampleAdversarialUsage uses the previous file to simply print the essays. 